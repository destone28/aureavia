# Database
DATABASE_URL=postgresql+asyncpg://aureavia:aureavia@localhost:5433/aureavia

# Security
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Email (2FA)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@aureavia.com

# Redis
REDIS_URL=redis://localhost:6379/0

# Booking API (external)
BOOKING_API_KEY=your-booking-api-key
BOOKING_API_URL=https://api.booking.com/v1

# App
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]

# Development mode (log 2FA codes to console instead of sending email)
DEV_MODE=true
